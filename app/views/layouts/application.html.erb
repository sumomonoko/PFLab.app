<!DOCTYPE html>
<html>
  <head>
    <title>PFLabApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
<script src="https://kit.fontawesome.com/7a4d66770d.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <style>
      #notice {
      color:#ab8048
      }
      body {
        margin: 0;
        padding: 0;
        position: relative;
      }
      header {
        width: 100%;
      }
      .content {
        min-height: calc(100vh - 75px);
      }
      footer {
        width: 100%;
        height: 75px;
      }
    </style>
    <div class="content">
      <%= render 'layouts/header' %>
      <main>
        <div class="container">
          <div class="row w-100 justify-content-end pt-4">
            <% if user_signed_in? %>
              <%= render 'public/searches/serach' %>
            <% end %>
          </div>
        </div>
        <div style="height: 100px;" >
          <p class="h5 text-center mt-3" id="notice"><%= notice %></p>
        </div>
        <%= yield %>
        <p id="page-top"><a href="#">Page Top</a></p>
      </main>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
